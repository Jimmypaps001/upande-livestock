[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations
upande_livestock.patches.rename_livestock_doctypes
upande_livestock.patches.preserve_event_activity_cost
# Must run BEFORE model sync: sync_all imports workspace/livestock/livestock.json,
# and a rename run after that import would be renaming the old workspace onto the
# new one the import had just created. Closes the name/title split that aimed the
# desk card at /desk/livestock, a route no workspace answered to.
upande_livestock.patches.livestock_workspace_matches_its_name

# Herds carried two Link-to-Cost-Center fields both labelled "Cost Center", and
# nothing read either for feed, milk or drugs. Folds them into `cost_center`
# (carrying the custom twin's value where the survivor is blank) and moves the
# flat Livestock Settings default into a per-company row. Must run BEFORE model
# sync: it reads `custom_cost_center`, which that sync drops.
upande_livestock.patches.one_cost_centre_per_herd

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
upande_livestock.patches.rename_diagnosis_disease_field
upande_livestock.patches.rename_livestock_event_docs
upande_livestock.patches.backfill_health_timeline_events
upande_livestock.patches.migrate_animals_off_asset
upande_livestock.patches.repair_zeroed_age_interval_settings
# Must run after migrate_animals_off_asset: it resolves Livestock Event.animal
# as an Animal name (frappe.db.get_value("Animal", row.animal, ...)), and
# migrate_animals_off_asset is what repoints that field from an Asset name to
# an Animal name in the first place. Reordering this earlier would make the
# lookup silently miss (row.animal still an Asset name), logging "Linked
# Animal ... no longer exists" and skipping every row instead of raising.
upande_livestock.patches.backfill_calf_fields_from_animal
upande_livestock.patches.backfill_animal_disabled
# No ordering dependency on anything above: works directly off event_date/
# service_date/diagnosis_date, none of which are touched by another patch.
# Must run before LivestockEvent.validate()'s new mandatory event_date check
# is exercised on these 2 rows' next save (it only binds on new documents, so
# this is a data-quality fix, not a precondition for migrate to succeed).
upande_livestock.patches.backfill_event_date_from_twin_field
upande_livestock.patches.migrate_milking_session_to_time
upande_livestock.patches.align_event_date_with_type_date
upande_livestock.patches.namespace_livestock_roles
upande_livestock.patches.flag_drug_consuming_event_types
upande_livestock.patches.create_livestock_stock_entry_types
# Must run after the DocType sync that ships _validate_pregnancy_link, so the
# repair is not racing the write path that caused the corruption. Repoints
# Calving/Abortion pregnancy links from a Pregnancy Diagnosis to the Service it
# confirmed; every reader of that field joins it against a Service.
upande_livestock.patches.relink_pregnancy_to_service
# The livestock roles could not read Item, so the concentrate plan and the drug
# picker were closed to a user holding only those roles — which is what a farm
# hand holds. Read only, and only for the two roles that run and plan the store.
upande_livestock.patches.grant_livestock_item_read
upande_livestock.patches.add_backdating_fields
# Stamps custom_herd/custom_is_livestock_feed/custom_ration_kind on the BOMs
# the herds already point at (Herds.bom), as "Standing". Must run after the
# custom_field.json fixture sync creates those BOM fields.
upande_livestock.patches.backfill_standing_ration_boms
# The two feed mixes were both posting as the bare "Manufacture" type, so the
# ledger could not tell the concentrate mill from the mixer wagon. Names them,
# alongside SCP's "Chemical Mixing"; _engine._run_manufacture then asks
# common/stock.stock_entry_type_for() which of the two a run is.
upande_livestock.patches.create_feed_mixing_stock_entry_types
# Three sidebar entries for one app, and desk blocks duplicating screens the
# React app now has. One workspace, one block of links. Frappe never removes a
# Workspace or a Custom HTML Block it has stopped shipping, so the fixture
# files going away is not enough.
upande_livestock.patches.retire_livestock_desk_blocks

# The code that writes a pregnancy check onto the service it answers lived in
# before_insert, which runs before the auto-link that finds that service — so it
# never ran. Thirty-four confirmed diagnoses, not one service marked Confirmed,
# and a farm that reads as having no pregnant cows because carrying_animals()
# asks the service.
upande_livestock.patches.settle_services_their_diagnoses_answered

# The desk calls this app Livestock. Two of the three names live in site records
# Frappe never re-ships (the Workspace Sidebar exports rather than imports, the
# Desktop Icon is never shipped at all), so a fixture edit alone leaves every
# migrated site on the old text. The workspace name it left behind is moved by
# livestock_workspace_matches_its_name above; this one's Workspace branch is a
# no-op by the time it runs on a site that gets both.
upande_livestock.patches.call_it_livestock_on_the_desk
