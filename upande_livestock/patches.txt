[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations
upande_livestock.patches.rename_livestock_doctypes
upande_livestock.patches.preserve_event_activity_cost

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
upande_livestock.patches.rename_diagnosis_disease_field
upande_livestock.patches.rename_livestock_event_docs
upande_livestock.patches.backfill_health_timeline_events
upande_livestock.patches.migrate_animals_off_asset
upande_livestock.patches.repair_zeroed_age_interval_settings
# Must run after migrate_animals_off_asset: it resolves Livestock Event.animal
# as an Animal name (frappe.db.get_value("Animal", row.animal, ...)), and
# migrate_animals_off_asset is what repoints that field from an Asset name to
# an Animal name in the first place. Reordering this earlier would make the
# lookup silently miss (row.animal still an Asset name), logging "Linked
# Animal ... no longer exists" and skipping every row instead of raising.
upande_livestock.patches.backfill_calf_fields_from_animal
upande_livestock.patches.backfill_animal_disabled
# No ordering dependency on anything above: works directly off event_date/
# service_date/diagnosis_date, none of which are touched by another patch.
# Must run before LivestockEvent.validate()'s new mandatory event_date check
# is exercised on these 2 rows' next save (it only binds on new documents, so
# this is a data-quality fix, not a precondition for migrate to succeed).
upande_livestock.patches.backfill_event_date_from_twin_field
upande_livestock.patches.migrate_milking_session_to_time
upande_livestock.patches.align_event_date_with_type_date
upande_livestock.patches.namespace_livestock_roles
upande_livestock.patches.flag_drug_consuming_event_types
upande_livestock.patches.create_livestock_stock_entry_types
# Must run after the DocType sync that ships _validate_pregnancy_link, so the
# repair is not racing the write path that caused the corruption. Repoints
# Calving/Abortion pregnancy links from a Pregnancy Diagnosis to the Service it
# confirmed; every reader of that field joins it against a Service.
upande_livestock.patches.relink_pregnancy_to_service
